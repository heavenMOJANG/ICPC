#pragma GCC optimize(3, "Ofast", "inline")
#include <bits/stdc++.h>
#define int long long
using namespace std;
constexpr int INF = -4e18;
void solve() {
    int n, m; cin >> n >> m;
    vector<int> p(n + 1), v(n + 1);
    for (int i = 1; i <= n; ++ i) cin >> p[i] >> v[i];
    vector<int> suf((n + 10) * (m + 10), INF);
    
    return;
}
signed main() {
    cin.tie(nullptr) -> sync_with_stdio(false);
    int _ = 1; //cin >> _;
    while(_ --) solve();
    return 0;
}