#include<bits/stdc++.h>
#define int long long
using namespace std;
mt19937_64 rnd;
signed main(){
    cout.tie() -> sync_with_stdio(0);
    int n = 10;
    vector<int> a(2 * n);
    iota(a.begin(), a.end(), 1);
    int cnt{}, flag = rdn() % 65536;
    do {
        if (cnt == rnd() %)
    } while (next_permutation(a.begin(), a.end()));
    return 0;
}
