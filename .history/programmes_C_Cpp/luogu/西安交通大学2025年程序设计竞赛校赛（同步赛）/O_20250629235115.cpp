#include<bits/stdc++.h>
#define int long long
using namespace std;
void solve(){
    int a, c, p; cin >> a >> c >> p;
    cout << (p - 1) * (p - 1) << "\n";
    return;
}
signed main(){
    cin.tie(0) -> sync_with_stdio(0);
    int _ = 1; //cin >> _;
    while(_ --) solve();
    return 0;
}