#pragma GCC optimize(1)
#pragma GCC optimize(2)
#pragma GCC optimize(3, "Ofast", "inline")
#include <bits/stdc++.h>
#define int long long
using namespace std;
signed main() {
    cin.tie(0) -> sync_with_stdio(0);
    freopen("6.out", "r", stdout);
    freopen("sequence.out", "w", stdout);
    int x;
    for (int i = 0; i < 3e5; ++ i) cin >> x;
    cout << x << endl;
    fclose(stdout);
    fclose(stdout);
    return 0;
}